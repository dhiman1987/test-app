# React + TypeScript + Vite

This is a minimal React project.
This has -
- vite based test suite vitest configured instead if Jest
- a counter component to demostrate minimal functionality
- unit test for this counter component
- test suite configured to calculate coverage.
- very minimal css styling. I hate ugly html :(